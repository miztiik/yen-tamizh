"""Shared helpers for the yen-tamizh build-time pipeline.

Small, dependency-light utilities reused across the generate / solve / validate /
bake stages live here (``backend/utilities/`` per CLAUDE.md section 3 topology).
"""
