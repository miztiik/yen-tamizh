name: daily

# Daily bank generation. STUB for now: the real generator (backend generate ->
# frontend/public/ bank, then commit) lands in Row 13. This keeps the cron +
# manual trigger + contents:write permission wired so Row 13 only has to fill in
# the generate + commit steps. See docs/architecture/generator/pipeline.md.
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: bank generation (stub)
        run: echo "Daily bank generator lands in Row 13 (backend generate -> frontend/public/)."
